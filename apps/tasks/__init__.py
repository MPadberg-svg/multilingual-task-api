"""Tasks application package."""

# Note: Celery tasks are in tasks.py and auto-discovered by Celery.
# Do NOT import them here to avoid AppRegistryNotReady errors.
# They are discovered via config.celery.app.autodiscover_tasks().
